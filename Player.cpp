#include "Player.hpp"
#include "Board.hpp"
#include "City.hpp"

using namespace std;

namespace pandemic {
        // Player::Player(Board& board, const int city) {}
        // ~Player::Player() {}
        // virtual Player& Player::drive(const int city) {}
        // virtual Player& Player::fly_direct(const int city) {}
        // virtual Player& Player::fly_charter(const int city) {}
        // virtual Player& Player::fly_shuttle(const int city) {}
        // virtual Player& Player::bulid() {}
        // virtual Player& Player::discover_cure(const int color) {}
        // virtual Player& Player::treat(const int city) {}
        // virtual Player& Player::take_card(const int city) {}
        // virtual Player& Player::remove_cards() {}
        // string Player::role() {}
}
